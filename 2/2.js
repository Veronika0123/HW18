    let length = +prompt('Сторона ромба в см.');
    console.log(`Сторона ромба ${length} см.`);

    let n = (length*length)*6;
    console.log(`Площадь ромба ${n} см^2.`);

    let volume = (length**3);
    console.log(`Объем ромба ${volume} см^3.`);
