    let length = +prompt('Ребро куба в см.');
    console.log(`Ребро куба ${length} см.`);

    let n = (length*length)*6;
    console.log(`Площадь куба ${n} см^2.`);

    let volume = (length**3);
    console.log(`Объем куба ${volume} см^3.`);
